services:
  redis:
    image: redis:7-alpine
    container_name: jarvis-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - ./data/redis:/data
    networks:
      - jarvis-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  postgres:
    image: pgvector/pgvector:pg16
    container_name: jarvis-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: ${TZ}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - jarvis-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 3s
      retries: 5

  adminer:
    image: adminer:latest
    container_name: jarvis-adminer
    restart: unless-stopped
    ports:
      - "127.0.0.1:${ADMINER_PORT}:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    networks:
      - jarvis-net
    depends_on:
      - postgres

  watchtower:
    image: nickfedor/watchtower:latest
    container_name: jarvis-watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      TZ: ${TZ}
      WATCHTOWER_CLEANUP: "true"
      WATCHTOWER_SCHEDULE: "0 0 4 * * *"
    networks:
      - jarvis-net

  dashboard:
    build: ./dashboard
    container_name: jarvis-dashboard
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./vault:/app/vault
    ports:
      - "100.89.106.8:8090:8090" 
      - "127.0.0.1:8090:8090"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - jarvis-net

  jarvis-core:
    build: ./orchestrator
    container_name: jarvis-core
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./skills-lib:/app/skills-lib:ro
      - ./vault:/app/vault
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - jarvis-net

  jarvis-ceo:
    build: ./bots/ceo
    container_name: jarvis-ceo
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./vault:/app/vault
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - jarvis-net

  jarvis-marketing:
    build: ./bots/marketing
    container_name: jarvis-marketing
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./vault:/app/vault
      - ./skills-repo/skills:/app/skills:ro
      - ./brand:/app/brand:ro
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - jarvis-net

  camofox:
    build: ./camofox
    container_name: jarvis-camofox
    restart: unless-stopped
    environment:
      - CAMOFOX_CRASH_REPORT_ENABLED=false
    networks:
      - jarvis-net

  jarvis-immo:
    build: ./bots/immo
    container_name: jarvis-immo
    restart: unless-stopped
    env_file: .env
    depends_on:
      - redis
      - postgres
    networks:
      - jarvis-net

  jarvis-seo:
    build: ./bots/seo
    container_name: jarvis-seo
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./vault:/app/vault
    depends_on:
      - redis
      - postgres
    networks:
      - jarvis-net

  jarvis-telegram:
    build: ./bots/telegram
    container_name: jarvis-telegram
    restart: unless-stopped
    env_file: .env
    depends_on:
      - redis
      - postgres
    networks:
      - jarvis-net

networks:
  jarvis-net:
    driver: bridge
